SELECT *
FROM dataset_1; -- The most basic query, this is used to pull ALL the data from a database.

SELECT passanger, temperature
FROM dataset_1; -- This will only select the weather and temperature columns from the dataset. 
